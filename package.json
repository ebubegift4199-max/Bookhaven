{
  "name": "bookhaven",
  "version": "1.0.0",
  "description": "BookHaven bookstore — Express + SQLite backend serving the static frontend",
  "main": "server.js",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "export:books": "node scripts/export-static-books.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
